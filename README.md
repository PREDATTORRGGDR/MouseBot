# MouseBot

Mouse recorder for Windows.

## Run

```powershell
python mouse_recorder.py
```

## Hotkeys

- `F8` - start or stop recording
- `F6` - play recorded mouse actions
- `Esc` - exit

The recording is saved locally as `mouse_pattern.json`.
